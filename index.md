## SuperEM电法仪程序

#### 升级后版本号1.00.1205
#### 升级前版本号1.00.1127

#### 发射机部分

1. 移除“使用GPS同步时间”，标题改为“系统时钟同步”，按钮改为“同步时间”；
2. 移除“滤波器设置”；
3. **移除状态页的“FIFO状态”“驱动模块状态”“过流”“急停状态”，改为醒目的提示**；
4. **加入脉冲设置**；
5. “预览设置”改为“电流电压系数”；

#### 接收机部分

1. 移除“使用GPS同步时间”，将另一个按钮放入“其他”；
2. “数据存储模式”移到最上方；
3. 移除“初始化和关闭4G模块”；
4. “4G模块SOCKET设置”改为“4G网络设置”；
5. 设置页面中的“网络设置”改为“Wi-Fi设置”；

#### 切换器部分

1. 时间不需要一直出现，改为手动查询并在消息栏提示；
2. 状态参数移入Ribbon中，总开关、总配置组数、大组号、小组号、有效通道一栏，名为“电极信息”。
